version: '3.8'

services:
  backend:
    build: .
    ports:
      - '3001:3001'
    environment:
      - PORT=3001
    volumes:
      # Persist the SQLite database file between container restarts
      - db-data:/app
    restart: unless-stopped

volumes:
  db-data:
